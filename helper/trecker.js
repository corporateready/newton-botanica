import '../loader'
import jQuery from 'jquery';

export default function Tracker() {
    // jQuery(document).ready(function() {
    //     let __int = setInterval(function() {
    //         if (window.roistat !== undefined && window.roistat.callTracking !== undefined && window.roistat.callTracking.phone !== undefined) {
    //             clearInterval(__int)
    //             let phone = ''
    //             jQuery('a[href*="tel"]').each(function(key, val) {
    //                 if (!phone.length) {
    //                     phone = jQuery(val).find('span span').text().replace(/[^+\d]/g, '')
    //                 }
    //                 jQuery(val).attr('href', 'tel:' + phone)
    //             })
    //         }
    //     }, 333)
    // })
}